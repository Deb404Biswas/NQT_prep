#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    int n,po;
    cout<<"Enter the number and the power it needs to be raised to"<<endl;
    cin>>n>>po;
    cout<<"The power is "<<pow(n,po);
}